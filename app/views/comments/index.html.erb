<h1>login</h1>

<%= if user_signed_in? %>
  <h4>mail:<% current_user.email %></h4>
  <%= link_to "logout",destroy_user_session_path, method: :destroy %>
  <% else %>
  <h2>loginしていません</h2>
  <%= link_to "login", new_user_session_path, class: 'post' %>
  <%= link_to "new", new_user_registration_path, class: 'post' %>
<% end %>
